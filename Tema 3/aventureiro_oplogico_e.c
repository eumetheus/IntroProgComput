#include <stdio.h>

int main() {

    // Definindo variáveis

    int a = 5;
    int b = 10;

    // Testando operador && (E)

    if (a > 0 && b > 0) {
        printf("Ambos os números são positivos\n");
    }

    return 0;
}